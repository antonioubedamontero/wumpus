import { TranslateDirectionPipe } from './translate-direction.pipe';

describe('TranslateDirectionPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateDirectionPipe();
    expect(pipe).toBeTruthy();
  });
});
